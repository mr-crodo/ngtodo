export class Category {
    id: number;
    title: string;
    icon: string;

    constructor(id: number, title: string, icon: string) {
        this.id = id;
        this.title = title;
        this.icon = icon;

    }
}
